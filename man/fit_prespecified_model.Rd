% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_prespecified_model}
\alias{fit_prespecified_model}
\title{Fit Prespecified Model}
\usage{
fit_prespecified_model(
  data,
  dv_col,
  id_col,
  conc_col,
  delta_bl_col,
  trt_col = NULL,
  tafd_col = NULL,
  method = "ML",
  remove_conc_iiv = FALSE
)
}
\arguments{
\item{data}{A data frame containing C-QT analysis dataset}

\item{dv_col}{An unquoted column name for dependent variable measurements}

\item{id_col}{An unquoted column name for ID data}

\item{conc_col}{An unquoted column name for drug concentration measurements}

\item{delta_bl_col}{An unquoted column name for delta_bl values, e.g. deltaQTCFBL, deltaHRBL}

\item{trt_col}{An unquoted column name for treatment group}

\item{tafd_col}{An unquoted column name for time measurements}

\item{method}{Method for nlme::lme fitting (ML or REML)}

\item{remove_conc_iiv}{Logical, whether to remove IIV on concentration slope}
}
\value{
An nlme::lme model object with the prespecified C-QT model structure (fixed effects for concentration, baseline, optional treatment and time)
}
\description{
Generates nlme::lme model either prespecified or without TRT and TIME.
}
\details{
The model structure is:
\deqn{
  \Delta \text{QTcF}_{i,j,k} =
  \left( \theta_0 + \eta_{0,i} \right)
  + \left( \theta_1 + \eta_{1,i} \right) C_{i,j,k}
  + \theta_2 \left( \Delta\text{QTcF}_{i,j,k=0} - \text{QTcF}_{\text{baseline}} \right)\\
  + \theta_3 \text{TRTG}_j
  + \theta_4 \text{TAFD}_k
}

Where:
\itemize{
\item \eqn{\theta_0} = population intercept
\item \eqn{\theta_1} = population slope for concentration effect
\item \eqn{\theta_2} = coefficient for time-matched baseline correction
\item \eqn{\theta_3} = treatment group effect (optional, when trt_col provided)
\item \eqn{\theta_4} = time effect (optional, when tafd_col provided)
\item \eqn{\eta_{0,i}} = random intercept for subject i
\item \eqn{\eta_{1,i}} = random slope for subject i (omitted when remove_conc_iiv = TRUE)
\item \eqn{C_{i,j,k}} = drug concentration
}
}
\examples{

fit_prespecified_model(cqtkit_data_verapamil, deltaQTCF, ID, CONC, deltaQTCFBL, TRTG, TAFD)
}
