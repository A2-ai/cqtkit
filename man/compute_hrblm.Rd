% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{compute_hrblm}
\alias{compute_hrblm}
\title{Compute population mean baseline HR (HRBLM)}
\usage{
compute_hrblm(data, bl_data, by = NULL, rr_col = RR)
}
\arguments{
\item{data}{A data frame to attach the \code{HRBLM} column to (e.g., the
assembled analysis dataset).}

\item{bl_data}{A data frame of baseline ECG measurements to compute
\code{HRBLM} from (typically raw data filtered to baseline rows).}

\item{by}{Optional grouping columns as bare symbols, supplied as
\code{c(col1, col2, ...)} or a single symbol. Default \code{NULL}.}

\item{rr_col}{Unquoted name of the RR column in \code{bl_data} (default \code{RR}).}
}
\value{
\code{data} with an \code{HRBLM} column added (a constant scalar repeated
across all rows). If \code{HRBLM} already exists on \code{data}, returns \code{data}
unchanged.
}
\description{
Assembly-time helper for building a C-QT analysis dataset. Computes the
population mean baseline heart rate from raw baseline ECG data and
attaches it as a constant column (\code{HRBLM}) on \code{data}. Not intended for
use on already-assembled datasets — use \code{\link[=compute_delta_hrblm]{compute_delta_hrblm()}} for
that.
}
\details{
When \code{by} is supplied, the scalar follows the white-paper recipe: per
group, average \code{60000 / rr_col}; then take the mean of those per-group
means. Groups with any \code{NA} in \code{rr_col} are dropped before averaging,
and a warning is emitted. When \code{by = NULL}, the scalar is a single row-
level mean of \code{60000 / rr_col}.
}
\examples{
\dontrun{
raw_bl <- raw |> dplyr::filter(BASELINE == "Y")
analysis_data |> compute_hrblm(raw_bl, by = c(RANDID, EXTRT))
}
}
