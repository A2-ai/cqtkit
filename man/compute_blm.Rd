% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{compute_blm}
\alias{compute_blm}
\title{Compute a population mean baseline value (BLM)}
\usage{
compute_blm(data, bl_data, group_col, ecg_param_col, blm_col_name)
}
\arguments{
\item{data}{A data frame to attach the population mean baseline column to
(e.g., the assembled analysis dataset).}

\item{bl_data}{A data frame of baseline ECG measurements to compute the
population mean baseline from (typically raw data filtered to baseline
rows), one row per replicate.}

\item{group_col}{Grouping columns, as bare symbols or strings, supplied
as \code{c(col1, col2, ...)} or a single value.}

\item{ecg_param_col}{Unquoted name of the column in \code{bl_data} to average.}

\item{blm_col_name}{Name of the column added to \code{data}.}
}
\value{
\code{data} with the population mean baseline column added (a constant
scalar repeated across all rows). If that column already exists on \code{data},
returns \code{data} unchanged.
}
\description{
Assembly-time helper for building a C-QT analysis dataset. Averages
\code{ecg_param_col} within each \code{by} group, averages those group means, and
attaches the result to \code{data} as a constant column. Not intended for use on
already-assembled datasets, use \code{\link[=compute_delta_hrblm]{compute_delta_hrblm()}},
\code{\link[=compute_delta_qtcbblm]{compute_delta_qtcbblm()}} or \code{\link[=compute_delta_qtcfblm]{compute_delta_qtcfblm()}} for that.
}
\details{
\code{ecg_param_col} must already be on \code{bl_data}, computed per replicate.
Corrected QT is nonlinear in RR, so a correction applied after averaging does not give
the mean of the replicate corrected values. Add the columns with
\code{\link[=compute_hr]{compute_hr()}} and \code{\link[=compute_qtcb_qtcf]{compute_qtcb_qtcf()}} first. Groups with any \code{NA} in
\code{ecg_param_col} are dropped before averaging, and a warning is emitted.
}
\examples{
bl_data <- compute_qtcb_qtcf(
  cqtkit_data_bl_verapamil,
  qtbl_col = NULL,
  rrbl_col = NULL
)

compute_blm(
  dplyr::select(cqtkit_data_verapamil, -QTCFBLM),
  bl_data,
  group_col = c(ID, TRTG),
  ecg_param_col = QTCF,
  blm_col_name = "QTCFBLM"
)
}
