% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{compute_qtcbblm}
\alias{compute_qtcbblm}
\title{Compute population mean baseline Bazett-corrected QT (QTCBBLM)}
\usage{
compute_qtcbblm(data, bl_data, by = NULL, qt_col = QT, rr_col = RR)
}
\arguments{
\item{data}{A data frame to attach the \code{QTCBBLM} column to.}

\item{bl_data}{A data frame of baseline ECG measurements.}

\item{by}{Optional grouping columns as bare symbols, supplied as
\code{c(col1, col2, ...)} or a single symbol. Default \code{NULL}.}

\item{qt_col}{Unquoted name of the QT column in \code{bl_data} (default \code{QT}).}

\item{rr_col}{Unquoted name of the RR column in \code{bl_data} (default \code{RR}).}
}
\value{
\code{data} with a \code{QTCBBLM} column added (a constant scalar). If
\code{QTCBBLM} already exists on \code{data}, returns \code{data} unchanged.
}
\description{
Assembly-time helper for building a C-QT analysis dataset. Computes the
population mean baseline Bazett-corrected QT (\code{QT / sqrt(RR/1000)})
from raw baseline ECG data and attaches it as a constant column
(\code{QTCBBLM}) on \code{data}. Not intended for use on already-assembled
datasets — use \code{\link[=compute_delta_qtcbblm]{compute_delta_qtcbblm()}} for that.
}
\details{
When \code{by} is supplied, the scalar follows the white-paper recipe: per
group, average the Bazett-corrected QT; then take the mean of those
per-group means. Groups with any \code{NA} in \code{qt_col} or \code{rr_col} are
dropped before averaging, and a warning is emitted. When \code{by = NULL},
the scalar is a single row-level mean.
}
\examples{
\dontrun{
raw_bl <- raw |> dplyr::filter(BASELINE == "Y")
analysis_data |> compute_qtcbblm(raw_bl, by = c(RANDID, EXTRT))
}
}
